import { ThemeBackgroundColor } from '../models/Theme'

export const lightBackground: ThemeBackgroundColor = {
  default: '#fff',
  paper: '#fff',
}

export const darkBackground: ThemeBackgroundColor = {
  default: '#1f2e41',
  paper: '#0e1125',
}
